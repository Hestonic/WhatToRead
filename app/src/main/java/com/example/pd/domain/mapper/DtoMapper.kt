package com.example.pd.domain.mapper

import com.example.pd.data.api.WtrResponse
import com.example.pd.domain.model.DtoModel

object DtoMapper {
    // TODO: Написать mapper, когда будет известен API
    fun map(response: WtrResponse): DtoModel {
        return DtoModel("")
    }
}